# Railway Deploy Configuration
# This file is used by Railway to deploy the backend

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
